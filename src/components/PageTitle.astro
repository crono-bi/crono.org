---
import Default from '@astrojs/starlight/components/PageTitle.astro';

const isPlayground = Astro.url.pathname.startsWith('/playground');
---

{!isPlayground && <Default {...Astro.props}><slot /></Default>}
