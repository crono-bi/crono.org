---
title: "current_time"
---

La función `current_time` devuelve la hora actual del sistema como un valor de tipo `time`.

Esta función se comporta como una constante del sistema y no requiere paréntesis.

## Ejemplo

```crono-sql
select current_time resultado;
```
