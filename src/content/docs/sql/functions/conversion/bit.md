---
title: "bit"
---

La función `bit` convierte una expresión al tipo booleano (`bit`). Devuelve `1` si la expresión es verdadera o distinta de cero, y `0` en caso contrario.

## Ejemplo

```crono-sql
select bit(1) resultado;
```

El resultado es:

> 1
