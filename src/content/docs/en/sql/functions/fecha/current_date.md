---
title: "current_date"
---

La función `current_date` devuelve la fecha actual del sistema como un valor de tipo `date`.

Esta función se comporta como una constante del sistema y no requiere paréntesis.

## Ejemplo

```crono-sql
select current_date resultado;
```
