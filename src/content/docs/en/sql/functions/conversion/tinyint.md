---
title: "tinyint"
---

La función `tinyint` convierte una expresión al tipo entero de 1 byte sin signo. Admite valores entre 0 y 255.

## Ejemplo

```crono-sql
select tinyint('200') resultado;
```

El resultado es:

> 200
