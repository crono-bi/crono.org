---
title: "boolean"
---

La función `boolean` convierte una expresión al tipo booleano. Devuelve `1` si la expresión es verdadera o distinta de cero, y `0` en caso contrario.

## Ejemplo

```crono-sql
select boolean(1) resultado;
```

El resultado es:

> 1
